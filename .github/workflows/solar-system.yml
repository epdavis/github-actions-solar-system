name: Solar System Workflow

on: 
  workflow_dispatch:
  push:
    
jobs:
    unit-testing:
        name: Unit Testing
        runs-on: ubuntu-latest
        steps:
        - name: print env
          run: echo $KUBECONFIG